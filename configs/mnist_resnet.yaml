inherits: base.yaml

dataset:
  name: mnist
  image_size: 28
  channels: 1
  num_classes: null
  conditional: false

model:
  architecture: resnet
  in_channels: 1
  out_channels: 1
  hidden_channels: 256
  kernel_size: 3
  num_mid_layers: 6
  residual: true
  embedding_injection: every_layer
  padding: zeros